#include "main.h"

/**
 * *_strcat - concatenates two strings
 * @dest: copy to
 * @src: copy from
 * Return: Return pointer to dest
 */

char *_strcat(char *dest, char *src)
{
	int a;
	int b;

	i = 0;

	while (dest[a] != '\0')
	{

		a++
	}

	b = 0;

	while (scr[b] != '\0')
	{
		dest[a]  = src[b];

		a++;
		b++;
	}

	dest[a] != '\0'

	return (dest)
}

