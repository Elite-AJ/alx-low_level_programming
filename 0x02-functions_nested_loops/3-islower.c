#include "main.h"
#include "stdio.h"

/**
 * main - Entry point
 * _islower -> prints 10 times the a
 * Return: returns 1 or 0 condirionally
 */
int main(void)
{
	int c;

	return (c >= 'a' && c <= 'z');
}
