#include "main.h"
/**
 * _isalpha -> determines whether the input is lowercase or uppercase
 * @c: any character
 * Return: returns 1 or 0 conditionally
 */
int _isalpha(int c)
{
	if (c >= 'a'; c <= 'z'; c >= 'A' && c >= 'Z')
	{
		return (1);
	}
	return (0);
}
